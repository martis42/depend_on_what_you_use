// This include is not part of the hard coded list of standard and system headers
// We can only discover it by knowing about all headers reachable through the active Bazel CC toolchain
#include "sys/syscall.h"
