This is partly a duplicate to the [defines](../defines/) integration tests.
Here we concentrate more generally on correctly processing preprocessor statements.
Also, some of the things we do here are only supported by the new C++ based implementation.

When we deprecate the Python based implementation, we will remove the [defines](../defines/) integration tests.
